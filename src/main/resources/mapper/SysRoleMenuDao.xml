<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tools.st.mapper.SysRoleMenuDao">

<resultMap id="BaseResultMap" type="com.tools.st.entity.SysRoleMenu">
  <id column="id" jdbcType="BIGINT" property="id"/>
  <result column="role_id" jdbcType="BIGINT" property="roleId"/>
  <result column="menu_id" jdbcType="BIGINT" property="menuId"/>
</resultMap>

<sql id="Base_Column_List">
  id, role_id, menu_id
</sql>

<sql id="Condition_List">
  <if test="id != null"> srm.id = #{id, jdbcType=BIGINT} </if>
  <if test="roleId != null"> srm.role_id = #{roleId, jdbcType=BIGINT} </if>
  <if test="menuId != null"> srm.menu_id = #{menuId, jdbcType=BIGINT} </if>
</sql>

<sql id="Select_As_List">
  srm.id as id, srm.role_id as roleId, srm.menu_id as menuId
</sql>

<select id="selectByPrimaryKey" parameterType="Long" resultMap="BaseResultMap">
  select
  <include refid="Base_Column_List" />
  from sys_role_menu
  where id = #{id,jdbcType=BIGINT}
</select>

<delete id="deleteByPrimaryKey" parameterType="Long">
  delete from sys_role_menu
  where id = #{id,jdbcType=BIGINT}
</delete>

<insert id="insert" parameterType="com.tools.st.entity.SysRoleMenu">
  <selectKey keyProperty="id" order="AFTER" resultType="Long">
    SELECT LAST_INSERT_ID()
  </selectKey>
  insert into sys_role_menu (role_id, menu_id)
  values (#{roleId, jdbcType=BIGINT}, #{menuId, jdbcType=BIGINT})
</insert>

<update id="updateByPrimaryKey" parameterType="com.tools.st.entity.SysRoleMenu">
  update sys_role_menu
  set role_id=#{roleId, jdbcType=BIGINT}, menu_id=#{menuId, jdbcType=BIGINT}
  where id = #{id,jdbcType=BIGINT}
</update>

</mapper>